import 'net' as net
import 'thread' as th

server = net.Server (
    port = 8000
)

server.get ('/', fun (req, res) \ res.status (200).body ('<h1>Hello, World!</h1>'))

fun about (req, res)
    res.status (200).body ('<h1>About Us</h1>')
    return res

server.get ('/about', about)

server.serve ()