import '_Native_Socket' as s

sock = s.socket ()
s.bind (sock, 'localhost', 8000)
s.listen (sock, 10)

while 1
    try
        con_sock = s.accept (sock)
        req = s.read (con_sock)

        s.send (con_sock, "Hello")

    catch E
        write ("Error:", E)